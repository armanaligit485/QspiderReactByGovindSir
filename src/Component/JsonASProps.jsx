import React from 'react'

const JsonASProps = (props) => {
  return (
    <>
    <h1>Props As json</h1>
    <p>{props.name} {props.age} {props.gender} {props.PhoneNo}</p>
    </>
  )
}

export default JsonASProps
